var buttonAddTask = document.getElementsByClassName("profile_button-addTask");
var buttonStart = document.getElementsByClassName('profile_button-start');
var buttonPause = document.getElementsByClassName('profile_button-pause');
var buttonStop = document.getElementsByClassName('profile_button-stop');
var formTask = document.getElementsByClassName("new_task");

for (let i = 0; i < formTask.length; i++) {
  formTask[i].onsubmit = function(e) {
    var form = this;
    var buttonStart = form.getElementsByClassName("profile_button-start")[0];
    console.log(buttonStart);
    buttonStart.style.display = "none";
    var buttonPause = form.getElementsByClassName("profile_button-pause")[0];
    buttonPause.style.display = "inline-block";
    console.log(buttonPause);
    var buttonStop = form.getElementsByClassName("profile_button-stop")[0];
    buttonStop.style.display = "inline-block";
    console.log(buttonStop);
  };
};