var buttonAddTask = document.getElementsByClassName("profile_button-addTask");
var buttonStart = document.querySelectorAll('.profile_button-start');
var buttonPause = document.getElementsByClassName('profile_button-pause');
var buttonStop = document.getElementsByClassName('profile_button-stop');

for (let i = 0; i < buttonStart.length; i++) {
  buttonStart[i].onclick = function(e) {
    var closestButtonStart = this;
    closestButtonStart.style.display = "none";
    console.log(closestButtonStart);
    var closestButtonPause = closestButtonStart.nextElementSibling;
    closestButtonPause.style.display = "inline-block";
    console.log(closestButtonPause);
    var closestButtonStop = closestButtonPause.nextElementSibling;
    closestButtonStop.style.display = "inline-block";
    console.log(closestButtonStop);
  };
};