var nameCustomer = "<%= @customer.name %>";
var tableRow = document.querySelector('.admin-block-new-customer .admin-table-new-customer tbody');
var newCustomer = document.createElement('tr');
var customerInputTextError = document.getElementsByClassName("customer-input-text-error")[0];
var customerInput = document.getElementsByClassName("customer-third-class")[0];


newCustomer.innerHTML = ('<tr>' +
  '<th>' + nameCustomer + '</th>' +
  '<th class="admin-table-links">' +
  '<a href="#">Edit</a>' +
  '<a href="#">Delete</a>' +
  '</th>' +
  '</tr>'
);
tableRow.appendChild(newCustomer);

customerInputTextError.style.display = "none";
customerInput.value = "";
customerInput.className = "project-third-class admin-create-customer";


var nameProject = "<%= @project.name %>";
var tableRow = document.querySelector('.admin-block-new-project .admin-table-new-project tbody');
var newProject = document.createElement('tr');
var projectInputTextError = document.getElementsByClassName("customer-input-text-error")[0];
var projectInput = document.getElementsByClassName("admin-create-user-text")[0];

newProject.innerHTML = ('<tr>' +
  '<th>' + nameProject + '</th>' +
  ' <th>Customer</th>' +
  '<th class="admin-table-links">' +
  '<a href="#">Edit</a>' +
  '<a href="#">Delete</a>' +
  '</th>' +
  '</tr>'
);
tableRow.appendChild(newProject);

projectInputTextError.style.display = "none";
projectInput.value = "";
projectInput.className = "project-third-class admin-create-customer";