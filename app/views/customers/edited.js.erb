let overlay = document.querySelector(".overlay");
let modal = document.querySelector(".modal");
modal.innerHTML = "<%= escape_javascript(render 'form_edit') %>";
overlay.classList.add('open');
modal.classList.add('open');
